# ============================================
# SECURITY WARNING: Change these values!
# ============================================

# JWT Secret Key (REQUIRED!)
# Generate a strong random key: openssl rand -base64 64
# Production will FAIL to start without this being set!
JWT_SECRET=

# Database Configuration (Optional - defaults provided)
POSTGRES_DB=cloudsync
POSTGRES_USER=cloudsync
POSTGRES_PASSWORD=cloudsync_password

# API URLs for Frontend (Optional - defaults provided)
# API_URL: Server-side Next.js rewrites (use internal Docker hostname)
# NEXT_PUBLIC_API_URL: SSR data fetching (use internal Docker hostname)
# In Docker: Use http://backend:8080 for both
# For local manual dev (no Docker): Use http://localhost:8080
API_URL=http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:8080
